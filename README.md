# OANDA Trade Mirror

Automatically mirror trades from a source OANDA account to one or more mirror accounts using the OANDA REST API.

## Setup

1. Install dependencies:
```bash
pnpm install
```

2. Configure environment variables in `.env`.

3. Start the app:
```bash
pnpm start
```